# Runtime installation is defined in pyproject.toml.
# Keep this compatibility file aligned with package metadata:
-e .[all]

# CLIP is an optional upstream package installed from its official repository:
# pip install git+https://github.com/openai/CLIP.git
# Dynamic rendering also needs: playwright install chromium
